name: three workflow
on: push

jobs:
  run-first-command:
    runs-on: ubuntu-latest
    steps:
      - name: list files and cope repo
        run: ls -la
      - uses: actions/checkout@v7
      - name: Files list
        run: ls -la

